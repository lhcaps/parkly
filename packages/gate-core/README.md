# @parkly/gate-core

Shared plate engine cho PR-02.

Export chính:
- `normalizePlate()`
- `classifyPlateFamily()`
- `formatPlateDisplay()`
- `validatePlateStrict()`
- `detectSuspiciousPlate()`
- `parsePlate()`
- `buildPlateCanonical()`
